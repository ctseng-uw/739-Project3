syntax = "proto3";

package hadev;

message Request {
  optional int64 addr = 1; 
  optional bytes data = 2;
}

message Reply {
  bool yeah = 1;
}

message Blank {}

service Heartbeat {
  rpc is_primary (Blank) returns (Reply) {}
  rpc RepliWrite (Request) returns (Reply) {}
}
